# Accounting Assistant 首次安装指南

这份指南写给第一次使用 Accounting Assistant 的会计同事。你不需要会命令行，也不需要手动打开虚拟环境。

## 你会拿到什么

发布包通常是一个文件夹，例如：

```text
AccountingAssistant
```

里面会有软件本体、安装脚本、OCR 组件、Excel 导出功能所需文件。

## 第一步：解压文件夹

1. 找到别人发给你的 `AccountingAssistant.zip`。
2. 右键点击它。
3. 选择 `全部解压缩`。
4. 建议解压到桌面，例如：

```text
桌面\AccountingAssistant
```

不要直接在压缩包里面运行软件。

## 第二步：运行首次安装脚本

1. 打开解压后的 `AccountingAssistant` 文件夹。
2. 打开里面的 `scripts` 文件夹。
3. 找到这个文件：

```text
bootstrap.cmd
```

4. 双击 `bootstrap.cmd`。
5. Windows 可能会弹出安全提醒。如果你确认这个软件来自可信来源，请选择继续运行。
6. 屏幕会出现一个黑色窗口。不要关闭它。

这个脚本会自动做这些事情：

- 创建软件专用的 Python 环境。
- 安装 OCR 所需组件。
- 检查 Ollama 是否安装。
- 检查本地 Qwen 模型是否存在。
- 如果模型不存在，会自动下载。

第一次安装可能需要很久，尤其是下载 Qwen 模型时。请保持网络连接，不要关闭窗口。

## 第三步：等待安装完成

当窗口里出现类似下面的文字，说明安装完成：

```text
Setup complete
```

这时可以关闭黑色窗口。

## 第四步：启动软件

回到 `AccountingAssistant` 文件夹，找到软件的 `.exe` 文件。

通常名字类似：

```text
AccountingAssistant.App.exe
```

双击它即可启动。

以后正常使用时，只需要双击这个 `.exe`，不需要再手动打开 PowerShell，不需要手动打开 Python，也不需要手动打开虚拟环境。

## 第一次打开为什么比较慢

第一次打开软件时，OCR 会初始化。这个过程可能需要几十秒到几分钟。

软件会显示类似：

```text
OCR worker warming up...
```

请等待它变成 ready 状态后再开始 Analyze。

## 如果提示 Ollama 没有启动

请先尝试重启电脑，然后重新打开 Accounting Assistant。

如果仍然失败：

1. 打开 `scripts` 文件夹。
2. 再次双击 `bootstrap.cmd`。
3. 等它完成。
4. 再重新打开软件。

## 如果提示 Excel 格式不支持

这个软件只会写入自己的标准 Excel 格式。

如果你选择了一个旧 Excel 文件，而它不是标准格式，软件会拒绝写入，避免把数据写错列。

你可以选择：

- 新建一个 Excel 导出文件。
- 或者让别人把旧 Excel 整理成软件要求的标准格式。

## 如果你完全不知道哪里出错

请把以下信息截图发给维护人员：

1. 软件主界面的状态提示。
2. `Debug dump` 里最后几行内容。
3. 如果是安装阶段失败，把黑色安装窗口截图。

不要自己删除 `runtime`、`worker`、`data` 文件夹。
